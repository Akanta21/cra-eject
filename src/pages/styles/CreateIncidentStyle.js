import { makeStyles } from '@material-ui/core/styles';

const CreateIncidentStyles = makeStyles(theme => ({
  form: {
    width: '100%',
    marginTop: theme.spacing(1),
  },
}));

export default CreateIncidentStyles